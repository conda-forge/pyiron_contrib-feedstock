{% set name = "pyiron_contrib" %}
{% set version = "0.1.19" %}
{% set python_min = "3.10" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/pyiron_contrib-{{ version }}.tar.gz
  sha256: 4d25742de909f7ff2c63c2d4678904df054b818e7bdd0e57f40f89f1970d8066

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - pip
    - setuptools
  run:
    - pyiron_snippets >=0.1.3
    - python >={{ python_min }}
    - matplotlib-base >=3.9.4
    - numpy >=1.26.4  # for numba compatibility
    - pyiron_base >=0.13.0
    - scipy >=1.15.0
    - seaborn >=0.13.2
    - pyparsing >=3.1.4

test:
  imports:
    - pyiron_contrib
  commands:
    - pip check
  requires:
    - pip
    - boto3
    - python {{ python_min }}

about:
  home: https://github.com/pyiron/pyiron_contrib
  summary: Repository for user-generated plugins to the pyiron IDE.
  license: BSD-3-Clause
  license_file: LICENSE

extra:
  recipe-maintainers:
    - niklassiemer
    - pyiron-runner
    - jan-janssen
    - pmrv
    - srmnitc
